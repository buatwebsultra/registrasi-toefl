# Deny all direct web access to storage directory
# This directory should only be accessed through Laravel's storage facade
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
